# The state file is per-fleet operational data, never the repo's business.
.fleet-state.json
examples/.fleet-state.json
examples/sandbox/
__pycache__/
*.pyc

# Internal spec. Never publishes — this repo is public.
PRODUCT.md
